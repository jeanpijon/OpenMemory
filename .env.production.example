# OpenMemory Production Configuration
# Copy this to .env.production and customize

# PostgreSQL
POSTGRES_PASSWORD=your_secure_postgres_password_here

# OpenMemory API
OM_PORT=8080
OM_MODE=standard
OM_API_KEY=  # Leave empty for VPN/private network, or set for auth

# Dashboard
DASHBOARD_PORT=3000
DOMAIN=memory.yourdomain.com  # Your domain for dashboard API URL

# Rate Limiting (recommended for production)
OM_RATE_LIMIT_ENABLED=true

# Ollama Embedding Model
# After deployment, run: docker compose -f docker-compose.production.yml exec ollama ollama pull nomic-embed-text
# Or use: mxbai-embed-large, all-minilm, etc.
